#include <math.h>
#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    float sum = 0;
    for (float i = 0; i < n; i++)
    {
        while (n != -1)
        {
            break;
        }
        sum += i;
    }
    printf("%.1f ", sum/n);
}
