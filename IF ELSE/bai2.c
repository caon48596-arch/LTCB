#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    if (n > 100)
    {
        printf(" la so lơn hon 100");
    }
    else if (n < 100)
    {
        printf("la so be hơn 100");
    }
}